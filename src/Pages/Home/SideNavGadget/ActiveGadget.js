import React from 'react';
import TopProducts from '../../Shared/TopProducts';

const ActiveGadget = ({ active }) => {
    return (
        <TopProducts active={active} />
    )
};

export default ActiveGadget;